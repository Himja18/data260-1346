# data260-1346
## Code Review Instructions
 
When reviewing code in this repository, respond using ONLY a bulleted list.
Do not use any prose paragraphs, introductions, or conclusions — every
point must be its own bullet. Each bullet should:
 
- Identify a single specific issue, risk, or improvement opportunity
- Reference the exact line, function, or file where it applies
- Be no longer than one sentence
Do not:
- Restate what the code does before critiquing it
- Group multiple issues into one bullet
- Add a summary sentence at the end
- Use any heading other than the bullet list itself
If the code has no issues, respond with a single bullet:
- "No issues found."
## Verification (Part 4)
 
This instruction was tested against `hw1_client.py` using the local
qwen2.5:3b model via `src/model_client.py`. See report.pdf for the
verification transcript and notes on whether the model followed the
bullet-only constraint on the first attempt or required a follow-up
reminder.